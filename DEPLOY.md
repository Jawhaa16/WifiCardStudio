# WiFi Card Studio - Deployment Guide

Энэхүү вэб сайт нь "Client-side only" буюу сервер шаардлагагүй (Static Site) тул байршуулахад маш хялбар бөгөөд үнэ төлбөргүй хувилбарууд олон бий.

---

## Хувилбар 1: Vercel (Хэмгийн хялбар, Хурдан) 🏆

Энэ нь хамгийн шилдэг сонголт юм.

1. **Vercel.com** руу орж бүртгүүлнэ (GitHub-аар орвол амар).
2. **"Add New..."** -> **"Project"** сонгоно.
3. Хэрэв таны код GitHub дээр байгаа бол Repository-оо сонгоод **Import** дарна.
   * *Эсвэл:* Таны компьютер дээрх `WifiScan` фолдерыг **Vercel CLI** ашиглан `vercel` гэж бичээд л оруулж болно.
4. **Deploy** товчийг дарна.
5. 1 минутын дотор таны сайт `https://wifi-studio.vercel.app` (жишээ нь) гэсэн хаягаар дэлхий даяар нээлттэй болно.

---

## Хувилбар 2: Netlify (Drop & Drop)

Код бичихгүйгээр шууд байршуулах арга.

1. **Netlify.com** руу орж бүртгүүлнэ.
2. Нүүр хуудас дээр нь **"Drag and drop your site output folder here"** гэсэн хэсэг бий.
3. `WifiScan` фолдероо тэр цонх руу чирж оруулна.
4. Болдоо! Сайт шууд ажиллаж эхэлнэ. Та дараа нь Domain name (жишээ нь: wifistudio.mn) холбож болно.

---

## Хувилбар 3: GitHub Pages

Хэрэв та кодоо GitHub дээр хадгалдаг бол.

1. Кодоо GitHub Repository руу Push хийнэ.
2. Repository-ийн **Settings** -> **Pages** цэс рүү орно.
3. **Branch** хэсгээс `main` (эсвэл `master`) сонгоод **Save** дарна.
4. Хэдэн минутын дараа `https://username.github.io/repo-name` хаягаар орж болно.

---

## Хувилбар 4: Docker (Professional / Server)

Бид аль хэдийн `Dockerfile` болон `nginx.conf` бэлдсэн байгаа. Хэрэв танд өөрийн VPS (Virtual Private Server) эсвэл Cloud (Google Cloud Run, AWS, DigitalOcean) байгаа бол:

**Image үүсгэх:**
```bash
docker build -t wifi-card-studio .
```

**Ажиллуулах (Local):**
```bash
docker run -p 8080:80 wifi-card-studio
```
Ингээд `http://localhost:8080` дээр ажиллана.

**Cloud Run (Google Cloud) дээр байршуулах:**
Хэрэв та Google Cloud SDK суулгасан бол:
```bash
gcloud run deploy --source .
```
Энэ комманд нь автоматаар Build хийгээд, Google-ийн сервер дээр байршуулж HTTPS линк өгнө.

---

### Зөвлөмж
Та зүгээр л хүмүүст хэрэглүүлэх гэж байгаа бол **Хувилбар 1 (Vercel)** эсвэл **Хувилбар 2 (Netlify)**-г сонгохыг зөвлөж байна. Энэ нь үнэгүй, хурдан, бас автомат HTTPS хамгаалалттай.
